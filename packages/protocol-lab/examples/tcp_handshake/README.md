# TCP handshake

TCP is a reference definition, not an engine dependency. Protocol Hack's
`protocol_lab/specs/tcp_handshake_v1.yaml` can be loaded unchanged by this
package, demonstrating that the same public API handles a real protocol.
