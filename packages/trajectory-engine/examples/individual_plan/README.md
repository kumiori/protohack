# Individual plan

Use `build_plan_payload`, `build_trajectory_document`, and `trajectory_yaml` to
create a portable plan. Use `realize_primitive` to append actual history without
rewriting authored coordinates.
