# Independent shared-goal contributions

Wrap each canonical document with `build_goal_trajectory`. A goal owns a
collection of independently authored contributions, never one implicit shared
trajectory.
